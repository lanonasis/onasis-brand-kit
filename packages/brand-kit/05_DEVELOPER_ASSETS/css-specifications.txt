/* Brand Colors */
:root {
  --ln-navy: #1B365D;
  --ln-green: #00D4AA;
  --ln-gold: #FFD700; /* primary logo only */
}

/* Logo sizing guidelines */
.logo-primary { max-width: 200px; height: auto; }
.logo-secondary { max-height: 60px; height: 60px; width: auto; }
.logo-icon { width: 32px; height: 32px; }
.logo-monogram { width: 24px; height: 24px; }

/* Responsive example: swap to icon at small widths */
@media (max-width: 480px) {
  .header-logo { display: none; }
  .mobile-icon { display: block; }
}

/* Optional dark mode adjustments */
@media (prefers-color-scheme: dark) {
  :root { color-scheme: dark; }
  /* Example: adjust logo container backgrounds if used */
  .logo-container { background-color: #0e1b30; }
}
